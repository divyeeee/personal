/*To swap numbers in cyclic order using call by reference and to check prime number by creating a function. To find the
multiplication values and the cubic values using inline function. To calculate the area of circle, rectangle and triangle using
function overloading.*/
#include <iostream>
using namespace std;
void swapNum(int *arr,int size){
    int *last=(arr+size-1);
    for(int i=0;i<size;i++)
}